# Video Assets Folder for Production
Place your mp4/webm looping video files here (e.g., `carbon_loop.mp4`).
Vite will automatically copy files in the `/public` root directory to `dist/` upon build so they are hosted correctly.
